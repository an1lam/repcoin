/** @jsx React.DOM */

var React = require('react');
var Feed = require('./Feed.react');

var RepsApp = React.createClass({
  render: function() {
    return (
      <div className="repsApp">
        <h1>The first and best reputation platform.</h1>
        <Feed />
      </div>
    );
  }
});

module.exports = RepsApp;
